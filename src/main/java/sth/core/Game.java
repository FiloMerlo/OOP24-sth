package sth.core;

import sth.engine.entities.*;

import java.nio.*;


public class Game {
    public static void main(String[] args) {
        // Initialize the game
        Window window = new Window();
        
        double deltaTime = 0.016;
        // Start the game loop
        while (true) {
            // Update game state
            
            // Render the window
            // window.render();
        }
    }
}
